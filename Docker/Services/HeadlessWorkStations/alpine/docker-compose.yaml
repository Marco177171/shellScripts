services:
  ws:
    hostname: alpine
    build: .
    volumes:
      - volume:/root
    env_file: ./.env
    ports:
      - ${SSH_PORT}:22
    restart: unless-stopped

volumes:
  volume:

networks:
  debian-ws-net:
